# ── SABC Compliance Platform — environment variables ──────────────────────────
# Copy this file to .env and fill in your values.
# The deploy script ships .env to the server automatically if it exists.

# Port the platform is served on over HTTPS (primary entry point).
# A self-signed certificate is generated automatically on first launch.
HTTPS_PORT=443

# Plain-HTTP port. It only issues a 301 redirect to HTTPS. Keep it on the
# standard port 80 so the redirect to https://<host> (default :443) lands on
# the right port. Used by docker-compose.yml only.
HTTP_PORT=80

# Port the backend API is exposed on (for direct API / Swagger access)
BACKEND_PORT=3000

# Optional: the host machine's LAN IP so "Register this machine" works
# without manual typing on the Add VM page.  Leave blank to auto-detect.
HOST_IP=

# Optional: SSH admin user on the host (default: root).
# Set to your actual user if that is how you SSH into this machine.
HOST_ADMIN_USER=
